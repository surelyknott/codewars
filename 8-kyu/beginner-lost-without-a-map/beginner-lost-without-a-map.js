function maps(x) {
  // Pass a function to map and double the values
  let doubleArr = x.map((num) => num * 2);
  
  // Return the new array with doubled values
  return doubleArr;
}
​
// Test the function
console.log(maps([4, 5, 7, 9])); 
​
​
​